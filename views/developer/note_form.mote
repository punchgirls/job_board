<p>Add a personal note:</p>

<form id="note-form-{{ application.id }}" action="/note/{{ application.id }}"
  method="POST">
  <textarea name="note"></textarea><br/>

  <span id="countdown-msg-{{ application.id }}">
    200 characters remaining
  </span><br/>

  <button>Send</button>
</form>
