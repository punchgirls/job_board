% if session[:success]
  <p class="alert alert-success">
    {{ session.delete(:success) }}
  </p>
% end

% if session[:error]
  <p class="alert alert-error">
    {{ session.delete(:error) }}
  </p>
% end
