% if session[:success]
  <span class="alert success">
    {{ session.delete(:success) }}
  </span>
% end

% if session[:error]
  <span class="alert error">
    {{ session.delete(:error) }}
  </span>
% end
