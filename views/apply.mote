% if developer.nil?
  <a href="/apply/{{ post.id }}">
    apply
  </a>
% elsif developer && developer.applied?(post.id)
    <span>applied</span>
% else
  <span onclick="apply({{ post.id }})" id="post_{{ post.id }}">apply</span>
% end
